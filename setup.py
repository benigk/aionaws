"""
To build the local backend module as an installed package
To use the tag of'e' or 'develop' (the changes from backend module are s)
"""
from setuptools import setup, find_packages

setup(name='backend', version='1.0', packages=find_packages())